# Frontend

This directory contains the Vite + React UI for `diffx`.

## Commands

```sh
npm install
npm run dev
npm run build
npm run lint
```

`npm run build` writes the production bundle to `frontend/dist/`. The Go server embeds that directory at build time.
